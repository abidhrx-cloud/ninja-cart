# ninja-cart
This project implements a complete deep learning pipeline for multi-class image classification. The dataset is analyzed for class distribution and image dimension variability before preprocessing. Images are resized to uniform square dimensions and normalized to ensure stable model training. 
